{
    "extends": "../src/tsconfig.json",
    "compilerOptions": {
        "types": [
            "mocha",
            "chai",
            "sinon",
            "sinon-chai"
        ]
    },
    "typeAcquisition": {
        "enable": true
    },

    "exclude": [
        // Exclude test bundle files - they have complex require.context usage
        "*Bundle.js"
    ]

}